[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
# Intentional boundaries: FastAPI dependency defaults, guarded user tools,
# and bounded local blocking operations inside async upload endpoints.
ignore = ["BLE001", "B008", "ASYNC221"]
